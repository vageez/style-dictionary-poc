import StyleDictionary from 'style-dictionary';
/**
 * @name isClamp
 * @type filter
 * @description only returns tokens of type `shadow`
 */
export declare const isClamp: (token: StyleDictionary.TransformedToken) => boolean;
